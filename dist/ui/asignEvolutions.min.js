import { createEvolutionElement } from "../ui/createEvolutionElement.min.js"; import { fetchPokemonSprite } from "../api/fetchPokemonSprite.min.js"; async function asignEvolutions(e) { var t = document.getElementById("evolution-container"); if (t) { t.innerHTML = ""; let o = [{ species: e.chain.species, level: null, evolves_to: e.chain.evolves_to }]; if (e.chain.evolves_to && 0 !== e.chain.evolves_to.length) for (; 0 < o.length;) { var { species: l, level: n, evolves_to: i } = o.shift(), s = await fetchPokemonSprite(l.name) || "https://example.com/default.png", l = createEvolutionElement(l.name, s, n); t.appendChild(l), i && 0 !== i.length && i.forEach(e => { o.push({ species: e.species, level: 0 < e.evolution_details.length ? e.evolution_details[0].min_level : null, evolves_to: e.evolves_to }) }) } else t.innerHTML = "<p class='no-evolution-text'>No evolutions available.</p>" } } export { asignEvolutions };