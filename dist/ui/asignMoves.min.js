import { fetchMoveDetails } from "../api/fetchMoveDetails.min.js"; import { createMoveElement } from "../ui/createMoveElement.min.js"; function asignMoves(e) { if (e.moves && Array.isArray(e.moves)) { var t = e.moves.filter(e => e.version_group_details.some(e => "egg" === e.move_learn_method.name)), e = e.moves.filter(e => e.version_group_details.every(e => "egg" !== e.move_learn_method.name)); let o = document.getElementById("movements-container"); o.innerHTML = "", t.forEach(async e => { e = await fetchMoveDetails(e.move.url), e = createMoveElement(e, "- Egg-Move"); e.classList.add("movement-item"), o.appendChild(e) }), e.forEach(async e => { e = await fetchMoveDetails(e.move.url), e = createMoveElement(e, ""); e.classList.add("movement-item"), o.appendChild(e) }) } else console.error("asignMoves failed.") } export { asignMoves };